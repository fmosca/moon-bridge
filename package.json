{
  "name": "moonbridge-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "go run github.com/syumai/workers/cmd/workers-assets-gen && cp cmd/cloudflare/vendor/wasm_exec.js build/wasm_exec.js && tinygo build -o ./build/app.wasm -target wasm ./cmd/cloudflare",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
